后续可补充完善点

1) MVP4：加密密钥配置一致性校验
说明：多节点加密通信时，启动配置中的 aes_key_hex/hmac_key_hex 必须一致，否则会解密失败。
补充点：启动时增加一致性校验与错误提示，或在脚本/文档中统一声明同一套密钥。

2) MVP5（可选增强）：密钥管理与分发
说明：为不同节点提供独立密钥或采用主密钥派生机制。
补充点：引入简单密钥分发/协商流程（后续可用三方库或服务）。

3) MVP5（可选增强）：文件名下载封装
说明：在 manifest 下载之上增加“文件名 -> manifest”的索引查询。
补充点：下载时只提供文件名，系统自动查清单并逐片查找拼接。
